[tool.pytest.ini_options]
pythonpath = "src"
testpaths = ["tests/"]
asyncio_mode = "auto"

env = [
    "SERVER_NAME=Organization Test",
    "SERVER_HOST=http://test.gbdi.io",
    "ROUTER_URL=http://test.gbdi.io",
    "MONGO_HOST=localhost",
    "MONGO_USER=testuser",
    "MONGO_PASSWORD=mypassword",
    "MONGO_DB=organization",
    "MONGO_PORT=27017",
    "SUPERTOKENS_CONNECTION_URI=http://test.gbdi.io",
    "SUPERTOKENS_API_KEY=DHALFKJDHALFJHDAJF",
    "LOG_LEVEL=ERROR"
]

[tool.ruff]
line-length = 120
target-version = "py312"